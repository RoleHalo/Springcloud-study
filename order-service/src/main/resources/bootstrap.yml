spring:
  application:
    name: orderservice #服务名
  profiles:
    active: dev #环境
  cloud:
#    nacos:
#      server-addr: localhost:81 #nacos集群地址
      config:
        file-extension: yaml #配置文件的后缀名

